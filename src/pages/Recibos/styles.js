import styled from "styled-components";

export const Container = styled.div`

`;

export const FlexRow = styled.div`
    display: flex;
    gap: 0.5rem;
`;

export const Separator = styled.div`
    height: 0.5px;
    background: gray;
    margin: 2.5rem 0;
`;

export const RecibosContainer = styled.div`
    display: flex;
    gap: 2rem;
`;